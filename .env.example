# Lethe Configuration Example
# Copy to .env and fill in your values

# Telegram (required)
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_ALLOWED_USER_IDS=123456789

# LLM Provider - one of: openrouter, anthropic, openai, claude-max, codex
# If not set, auto-detects from available API keys
# LLM_PROVIDER=openrouter

# API Keys - set the one for your provider
# (claude-max and codex use OAuth, no API key needed)
OPENROUTER_API_KEY=your_key_here
# ANTHROPIC_API_KEY=your_key_here
# OPENAI_API_KEY=your_key_here

# For ChatGPT Plus/Pro subscription (Codex), no API key needed:
# 1. Set: LLM_PROVIDER=codex
# 2. Run: uv run lethe codex-login
# Tokens stored in ~/.lethe/codex_tokens.json (auto-refreshed)
# Or set CODEX_AUTH_TOKEN=<token> for access-token-only mode (no refresh)

# Model (optional, uses provider default if not set)
# LLM_MODEL=moonshotai/kimi-k2.5

# Heartbeat (optional)
# HEARTBEAT_INTERVAL=900
# HEARTBEAT_ENABLED=true

# Hippocampus memory recall (optional)
# HIPPOCAMPUS_ENABLED=true

# Debug logging (default: false)
# LLM_DEBUG=false
# LLM_DEBUG_DIR=logs/llm

# Claude Max OAuth (if using claude-max provider)
# Tokens are stored in ~/.config/lethe/claude_tokens.json
# On first run, you'll be prompted to authenticate via browser

# Anthropic OAuth (if using anthropic provider with OAuth)
# Run: uv run lethe oauth-login
# Tokens are stored in ~/.lethe/oauth_tokens.json
